# Spec-Driven Core — núcleo Traycer+

Carrega o protocolo canónico de orquestração spec-driven deste workspace.

**Ação:** Ler integralmente `.claude/skills/spec-driven-core/SKILL.md` e executar conforme o modo (Epic / Phases / Plan / Review / Verify / YOLO) e a delegação às skills `spec-*` descritos nesse ficheiro.

**Quando usar:** Planejamento estruturado, várias entregas, PRD/fases/tickets, verificação contra plano, automação end-to-end — ou quando quiseres forçar o núcleo explicitamente (o Cursor também pode ativar isto **sem** slash via `AGENTS.md` e a regra `spec-driven-core.mdc`).
